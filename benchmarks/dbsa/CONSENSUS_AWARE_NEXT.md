# Room consensus #2 — start from Aware again

Three voices: CS#1 (online learning), CS#2 (aggregation/dependence), AI architect.
PWDR stays on the reject pile. Christmas bow is not a crush path.
**Bar held:** crush full-source majority on raw prequential Brier.

---

## What they threw out

| Rejected | Why |
|---|---|
| PWDR / any `m+r` residual head | Made weather much worse; residual was the bomb |
| Christmas-bow-as-crush | Majority-attracting; caps at parity |
| Densify away selective stop | Flips sparsity-as-win into density-as-win |
| Hard ballot deletion of unread | Throws away the variance reduction majority keeps |
| Silent retune / knob soup | Spent beds; metrics held |

---

## The fight that resolved

- CS#2 wanted **no ballot deletion** (full-vote low variance).
- Architect wanted **selective ACI stop kept**, with unread re-entering `p`.
- CS#1 wanted **no majority blend** and **delay-corrected learning on disagreement**.

Merge: early-stop may thin the *active* set, but unread is not discarded — it
re-enters as an ESS-weighted **shadow** under the sheath gate. “Full mass”
means shadow-complete emission, not “all sources awake in ACI.”

---

## CONSENSUS object

### ESSC — ESS-Shadow Sheath Completion

*(also floated as SGSC-DH / SSED — same mechanism)*

**Predict** (after ACI selective recruitment / early-stop):

1. Active coalition forms the FE-certified side as today.
2. Diversified **unread** rows form a shadow log-odds pool:
   - copy/near-copies **block-collapse** (soft ESS, not hard delete from roster)
   - weight by ESS × existing diagnosticity / source-trust
3. Sheath gates a bounded credit of that shadow into emitted `p`
   — **including when shadow opposes** the active / majority bloc.
4. Null / silence paths untouched.
5. Christmas vote-blend **off** the crush path.

**Feedback** (queue-release only):

- Delay-corrected merit update on the sheath credit gate, emphasized on
  rounds where active∪shadow **disagreed** with majority (or active≠shadow).
- No separate whitening covariance. No residual stack on a base belief.

---

## Operational MUST properties

1. **Selective stop held** — mean sources used stays below full roster.
2. **No blend / no residual** — never mix toward unwhitened majority; no `m+r`.
3. **Unread enters `p`** — via ESS-weighted sheath shadow, not hard skip to zero.
4. **Disagreement learning** — gate merit updates focus on disagreement rounds.

---

## Shared falsifiable prediction

Pre-register before any ESSC look:

- Raw prequential Brier beats full-source majority by a predeclared margin
  on a fresh / locked bed.
- Gains concentrate where ESS-shadow opposes active/majority and the label
  matches the shadow.
- On exchangeable rounds (shadow ≈ active): within ε of better of
  {frozen Aware, majority}.
- Fail if gains appear only when already majority-aligned (bow reincarnated),
  or if ablating shadow removes the gain and replacing it with Christmas blend
  “recovers” it.

---

## Status

Consensus locked. **Implemented** on `AwareCoalitionCellular` (opt-in;
DBSA `AWARE_PARAMS` enables it). **First look scored:** weather does **not**
crush majority (0.1728 vs 0.1624); selective stop held (~3.54 sources).
Synthetic drift/crossover worlds beat majority — not a weather substitute.
See `REPORT_ESSC.md`. PWDR remains rejected. Metrics held.
