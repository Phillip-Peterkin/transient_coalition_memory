# Weather confirmation — AwareCoalitionCellular (Mnemosheath)

Written **before** scoring virgin `confirmation3`.

## Architecture under test

**Subject:** `tcm.AwareCoalitionCellular` — sealed ACI evidence laws plus
Mnemosheath (context awareness, empty curriculum, selective cue admission).

Parent freeze: finance confirmation8 ACI knobs, except Aware’s forced
`force_all_positive_null=False` (learned agreement diagnosticity replaces the
regime-blind unanimous-null stamp).

**Not under test:** Wave XI (`BatchedReserveCellular`). It does not enter the gate.

## Why this bed (not finance)

- Finance `confirmation8` is spent (ACI seal). No retune / re-look.
- Weather contact bed and `confirmation2` are spent (ACI looks + Aware contact peek).
- Aware’s development signal is trustworthy-source weather: learn whether
  unanimous model agreement is evidence or cheerleader null.
- Virgin slot: **`confirmation3`** — 12 cities disjoint from contact and confirmation2.

## Lineage baselines (same events)

1. Persistence oracle (`prev_truth`, else 0.5)
2. Memoryless majority of model votes (trustworthy-source ceiling; reported, not gated)
3. `SilenceEscapeCellular` — finance ancestor of ACI’s null channel
4. Sealed `ActiveExperimentalCellular` / ACI — parent architecture on this world

## Fixed knobs (no Weather retune)

ACI freeze (confirmation8):

- `min_delta=0.15`
- `max_silence_hazard=0.55`
- `null_rho_gain=0.30`
- `null_pe_floor=0.35`, `null_pe_span=0.50`, `null_err_beta=0.30`
- `fe_cert_slack=0.0`

Aware-only deviation (by class design, not a Weather fit):

- `force_all_positive_null=False`

Mnemosheath uses production defaults (selective admission; seeds `unanimous` +
`empty`). No Weather-fitted merit thresholds.

## Virgin universe

`confirmation3_universe.py` — 12 cities **disjoint** from:

- contact bed (`cities.py` / `data/`)
- confirmation2 (`confirmation2_universe.py` / `data_confirmation2/`)

Same calendar, models, label/vote rules as `PROTOCOL.md`.
Data cache: `data_confirmation3/`.

## Scoring

- First 70% of days: causal warm-up only (not gated)
- Last 30% holdout: scored **once**
- Feedback due at first event on decision day + 2

## Pass condition (holdout)

Claim PASS only if Aware satisfies **all**:

1. flip detection ≥ **0.45**
2. flip detection ≥ sealed ACI flip on this world (no regression vs parent)
3. flip detection ≥ silence-escape flip (no regression vs lineage ancestor)
4. overall accuracy ≥ persistence − **0.01**
5. prediction-up rate ≤ **0.65**
6. non-flip accuracy ≥ **0.50**

Majority is reported for regime honesty; beating majority is **not** required.

## Fail / honesty

Fail once. No retune on confirmation3. Contact peek and confirmation2 remain
spent and must not inform post-hoc threshold edits. This is a **new** clean
Weather bed confirmation of the awareness package, not recovery of the old
sandbox Weather final.
