# confirmation6 — invalid / spent

A confirmation score was observed on `data_confirmation6` after a download that
included delisted `MRO` and `PXD` with 100% null prices. That panel is not a
valid sealed universe.

No claim is made from those numbers. The sealed v2 look moves to
`confirmation7` with a price-coverage gate before scoring.
